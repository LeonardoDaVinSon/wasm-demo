{
  "name": "wasm-vs-ts-performance-demo",
  "version": "1.0.0",
  "description": "Demo so sánh hiệu năng giữa WebAssembly và TypeScript",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "wasm:build": "cd rust-wasm && wasm-pack build --target web",
    "wasm:install": "cd rust-wasm && cargo install wasm-pack"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vue-tsc": "^1.8.0"
  }
}
